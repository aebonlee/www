# 개발 로그 - 2026-03-05

## 작업자: Claude Opus 4.6

---

## 세션 1: Supabase 스키마/설정 점검 및 수동 처리 항목 도출

### 작업 배경

프론트엔드 코드와 Supabase DB 스키마 간 정합성을 점검하고, SQL 스크립트 또는 수동 처리가 필요한 항목을 도출.

### 점검 대상 파일

| 파일 | 역할 |
|------|------|
| `supabase_schema.sql` | 이커머스 DB 스키마 (products, orders, order_items) |
| `supabase_migration.sql` | 종합 마이그레이션 (user_profiles, blog, gallery, products, board, orders) |
| `supabase_admin_functions.sql` | 회원 관리 함수 (status, profile, delete, visited_sites) |
| `utils/supabase.js` | 프론트엔드 주문 CRUD |
| `utils/auth.js` | 인증 헬퍼 (OAuth, signup, profile) |
| `contexts/AuthContext.jsx` | 인증 상태 관리 + check_user_status RPC |

### 발견된 SQL 이슈 (4건)

| # | 항목 | 상세 |
|---|------|------|
| 1 | `orders` 테이블 컬럼 누락 | `cancelled_at TIMESTAMPTZ`, `cancel_reason TEXT` — 코드에서 사용하나 스키마에 미정의 |
| 2 | `orders.payment_status` CHECK 불일치 | 코드: `'refunded'` 사용 / 스키마: `('pending','paid','failed','cancelled')` |
| 3 | `orders` UPDATE RLS 정책 미존재 | 프론트엔드 주문 상태 변경 시 권한 에러 가능 |
| 4 | `user_profiles.role` CHECK 불일치 | 기본값 `'member'` / CHECK: `('user','admin')` |

### SQL 스크립트

```sql
-- 1. orders 누락 컬럼
ALTER TABLE public.orders
  ADD COLUMN IF NOT EXISTS cancelled_at TIMESTAMPTZ,
  ADD COLUMN IF NOT EXISTS cancel_reason TEXT;

-- 2. payment_status CHECK 보완
ALTER TABLE public.orders DROP CONSTRAINT IF EXISTS orders_payment_status_check;
ALTER TABLE public.orders ADD CONSTRAINT orders_payment_status_check
  CHECK (payment_status IN ('pending', 'paid', 'failed', 'cancelled', 'refunded'));

-- 3. orders UPDATE 정책
CREATE POLICY "orders_update" ON public.orders
  FOR UPDATE USING (
    user_id = auth.uid()
    OR EXISTS (SELECT 1 FROM public.user_profiles WHERE id = auth.uid() AND role = 'admin')
  );

-- 4. user_profiles role CHECK 보완
ALTER TABLE public.user_profiles DROP CONSTRAINT IF EXISTS user_profiles_role_check;
ALTER TABLE public.user_profiles ADD CONSTRAINT user_profiles_role_check
  CHECK (role IN ('member', 'admin'));
```

### 수동 처리 항목 (Supabase Dashboard)

| # | 항목 | 위치 |
|---|------|------|
| 5 | 하위 사이트 Redirect URL 추가 | Auth > URL Configuration |
| 6 | Google/Kakao OAuth 리다이렉트 URI 확인 | 각 Provider 콘솔 |
| 7 | allthat.dreamitbiz.com DNS 레코드 | 도메인 관리 |

**Redirect URLs 추가 목록:**

```
https://allthat.dreamitbiz.com/**
https://books.dreamitbiz.com/**
https://ahp-basic.dreamitbiz.com/**
https://competency.dreamitbiz.com/**
https://templete.dreamitbiz.com/**
```

---

## 세션 2: 하위 사이트 템플릿 클린 재구축

### 작업 배경

기존 `aebonlee/templete` 리포지토리의 템플릿이 다른 작업 중 변경되었던 것을 본사이트(`www.dreamitbiz.com`)의 **최신 인프라**를 반영하여 `D:\templete-ref\`에 클린 재구축하고 GitHub에 push.

**목표**: `src/config/site.js` 하나만 수정하면 새 하위 사이트를 즉시 배포할 수 있는 틀 제공.

### 이전 대비 주요 개선사항

| 항목 | 이전 | 이번 |
|------|------|------|
| ThemeContext | localStorage | **cookie** (본사이트 동기화) |
| CartContext | localStorage | **sessionStorage** (본사이트 동기화) |
| supabase.js fallback | localStorage | **in-memory 변수** |
| CommentSection | 미포함 | **포함** |
| commentStorage.js | 미포함 | **포함** |
| storage.js (이미지 업로드) | 미포함 | **포함** |
| ImageUpload.jsx | 미포함 | **포함** |
| useCountUp.js | 미포함 | **포함** |
| community.css | 미포함 | **포함** |
| toast.css | `var(--bg-gray)` 버그 | `var(--bg-light-gray)` **수정** |
| community 번역 키 | 누락 2건 | `community.cancel`, `community.loading` **포함** |
| productStorage.js | 본사이트 데이터 포함 | fallback **빈 배열** |
| SEOHead.jsx | 하드코딩 DreamIT Biz | **site.js SITE_CONFIG 참조** |
| translations.js ImageUpload 키 | 누락 | `auth.dragOrClick` 등 5개 키 **추가** |

### 변경 파일 목록

#### 신규 추가 (7파일)

| 파일 | 설명 |
|------|------|
| `src/components/CommentSection.jsx` | 댓글 섹션 컴포넌트 (본사이트 복사) |
| `src/components/ImageUpload.jsx` | 이미지 업로드 컴포넌트 (본사이트 복사) |
| `src/hooks/useCountUp.js` | 카운트업 애니메이션 훅 (본사이트 복사) |
| `src/utils/commentStorage.js` | 댓글 CRUD (본사이트 복사) |
| `src/utils/storage.js` | Supabase Storage 업로드 헬퍼 (본사이트 복사) |
| `src/utils/productStorage.js` | 상품 CRUD — fallback 빈 배열 (수정) |
| `src/styles/community.css` | 커뮤니티 스타일 (본사이트 복사) |

#### 수정 (7파일)

| 파일 | 변경 내용 |
|------|----------|
| `src/contexts/ThemeContext.jsx` | localStorage → cookie 기반 |
| `src/contexts/CartContext.jsx` | localStorage → sessionStorage 기반 |
| `src/utils/supabase.js` | localStorage fallback → in-memory `_memoryOrders` |
| `src/components/SEOHead.jsx` | 하드코딩 제거 → `site.js` SITE_CONFIG 참조 |
| `src/utils/translations.js` | ImageUpload 관련 auth 키 5개 추가 |
| `src/index.css` | `community.css` import 추가 |
| `README.md` | localStorage → sessionStorage 표기 수정 |

### 검증 결과

- `npm run build` — **성공** (110 modules, 1.87s)
- `grep -r "localStorage" src/` → **0건**
- `grep -r "publishingDetails|educationDetails" src/` → **0건**
- translations.js에 `community.cancel`, `community.loading` 키 존재 확인
- toast.css에 `var(--bg-gray)` 없음 확인
- 11개 컨텍스트/유틸/컴포넌트/훅 파일 본사이트와 일치 확인
- 9개 페이지 파일 본사이트와 일치 확인
- SEOHead가 site.js 기본값 사용 확인

### 배포 정보

- **리포지토리**: `aebonlee/templete` (force push, commit `c7a3fd0`)
- **도메인**: `templete.dreamitbiz.com`
- **배포 방식**: GitHub Pages (`.github/workflows/deploy.yml`)
- **빌드 도구**: Vite 7.3.1

### 프로젝트 구조 (최종)

```
templete-ref/
├── package.json, vite.config.js (port 5174), index.html
├── .env.example, .gitignore, README.md, CNAME
├── .github/workflows/deploy.yml
└── src/
    ├── main.jsx, App.jsx, index.css
    ├── config/       site.js, admin.js
    ├── contexts/     ThemeContext(cookie), LanguageContext, AuthContext,
    │                 CartContext(sessionStorage), ToastContext
    ├── utils/        supabase.js(in-memory), auth.js, portone.js,
    │                 translations.js, searchStorage.js, commentStorage.js,
    │                 productStorage.js(빈배열), storage.js
    ├── components/   layout/(Navbar, Footer), AuthGuard, SearchModal,
    │                 SEOHead(site.js참조), Pagination, CommentSection, ImageUpload
    ├── hooks/        useAOS.js, useCountUp.js
    ├── layouts/      PublicLayout.jsx
    ├── pages/        Home, Login, Register, ForgotPassword, MyPage,
    │                 Cart, Checkout, OrderConfirmation, OrderHistory, NotFound
    └── styles/       base, navbar, footer, hero, animations, shop, auth,
                      search, toast(수정됨), dark-mode, responsive, community, site
```

---

## 세션 3: 본사이트 toast.css 버그 수정

### 작업 배경

하위 사이트 템플릿 재구축(세션 2) 파일 비교 검증 과정에서 본사이트 `toast.css`에 존재하지 않는 CSS 변수를 참조하는 버그 발견.

### 발견된 이슈

| 파일 | 행 | 이전 | 수정 |
|------|-----|------|------|
| `src/styles/toast.css` | 70 | `var(--bg-gray)` | `var(--bg-light-gray)` |

- `--bg-gray`는 `base.css`에 정의되지 않은 변수 → `.toast-close:hover` 배경색이 적용되지 않음
- `--bg-light-gray`가 올바른 변수명 (base.css에 정의됨)
- 템플릿(`aebonlee/templete`)에는 이미 수정 반영되어 있었음

### 영향 범위

- `.toast-close:hover` 스타일 1건
- 토스트 알림 닫기 버튼 hover 시 배경색 미적용 → 수정 후 정상 표시

---

## 세션 4: 관리자 회원목록 개선

### 작업 내용

관리자 회원 관리 페이지(`/admin/users`) 테이블 개선 — 최종 로그인 날짜 표시 + 목록 단위 조정 + 가로폭 확장.

### 변경 파일

| 파일 | 변경 내용 |
|------|----------|
| `pages/admin/AdminUsers.jsx` | `last_sign_in_at` 컬럼 추가 (날짜 + 상대시간), `pageSize` 20→10 |
| `contexts/AuthContext.jsx` | `SIGNED_IN` 이벤트 시 `user_profiles.last_sign_in_at` 갱신 |
| `styles/admin.css` | `.admin-content` max-width 1400→1600px 확장 |

### last_sign_in_at 컬럼 상세

- 날짜(`YYYY-MM-DD`) + 상대시간(오늘/어제/N일 전/N주 전/N개월 전/N년 전) 2줄 표시
- 30일 이상 미접속 시 상대시간 빨간 텍스트로 강조
- hover 시 `YYYY-MM-DD HH:MM` 툴팁 표시

### Supabase 스키마 (사전 처리 완료)

```sql
ALTER TABLE public.user_profiles ADD COLUMN last_sign_in_at TIMESTAMPTZ;
```

### 테이블 컬럼 너비 조정

| 컬럼 | 이전 | 이후 |
|------|------|------|
| ID | 80px | 60px |
| 가입방법 | 100px | 90px |
| 가입일 | auto | 110px |
| 최종 로그인 | - | 140px (신규) |

### 커밋 이력

| 커밋 | 내용 |
|------|------|
| `21b34f8` | feat: 관리자 회원목록에 최종 로그인 날짜 컬럼 추가 |
| `7fb308d` | fix: 회원관리 페이지 목록 단위 20→10으로 변경 |

---

## 세션 5: 하위 사이트 템플릿 동기화

### 작업 배경

세션 4의 `AuthContext.jsx` 변경 및 `shop.css` 차이를 `aebonlee/templete` 리포에 반영.

### 동기화 결과 (39개 복사 대상 파일)

| 상태 | 파일 수 | 내용 |
|------|---------|------|
| 동일 | 37 | 이미 최신 |
| 수정 반영 | 2 | `AuthContext.jsx` (last_sign_in_at), `shop.css` (간격/폰트) |
| 누락 | 0 | 없음 |

### 변경 파일

| 파일 | 변경 내용 |
|------|----------|
| `contexts/AuthContext.jsx` | `SIGNED_IN` 이벤트 시 `last_sign_in_at` 갱신 로직 추가 |
| `styles/shop.css` | margin-top 12→16px, padding-top 12→14px, font-size 16→17px |

### 커밋 이력

| 커밋 | 리포 | 내용 |
|------|------|------|
| `38d9e33` | templete | feat: 로그인 시 last_sign_in_at 갱신 로직 추가 |
| `3574f42` | templete | fix: shop.css 본사이트 동기화 |

---

## 이전 세션 요약 (2026-03-04)

| 커밋 | 내용 |
|------|------|
| `4270d68` | allthat.dreamitbiz.com 가입자 표시 (AdminUsers colorMap + Footer Family Site) |
| `0683958` | localStorage 전면 제거 (ThemeContext→cookie, CartContext→sessionStorage, supabase.js→in-memory) |
