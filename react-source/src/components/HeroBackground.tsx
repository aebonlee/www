/**
 * HeroBackground — 히어로 배경 장식 레이어.
 * 떠다니는 사각 박스/도형·입자 등의 JS 장식 효과는 제거(클린 그라데이션 히어로).
 * 실제 히어로 배경(네이비→블루 그라데이션)은 .hero { background: var(--hero-bg) } CSS에서 처리됨.
 */
const HeroBackground = (_props: { slideIndex: number; isActive: boolean }) => null;

export default HeroBackground;
