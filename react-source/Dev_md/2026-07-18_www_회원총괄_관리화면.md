# 2026-07-18 www 회원 총괄 관리 화면 (/admin/members)

## 한 일
- `src/pages/admin/AdminMembers.tsx` 신규 — 전 사이트 통합 회원(www_profiles) 총괄 페이지.
  - 접근 제어 2중: 서버는 RLS(www_admins 정책), 화면은 진입 시 `rpc('www_is_admin')` 판정.
    비관리자는 데이터 요청 없이 안내 페이지("www_admins 등록 계정 전용") 표시.
    (기존 /admin/* 공통의 AdminGuard(이메일 기반 isAdmin)도 그 앞단에서 동작 — 3중인 셈)
  - 출처별 집계 카드: `www_signup_stats` 뷰 조회(사이트별 가입자수·완성/미완성·최근 가입일), total 내림차순.
  - 회원 목록 테이블: 이름·연락처·이메일·소속기관·수강과정·signup_site·signup_at·상태.
    PostgREST 1000행 제한 대비 range 페이징 루프로 전량 로드.
  - signup_site 셀렉트 필터 + 이름/이메일 검색, 미완성(is_complete=false) 행은 붉은 배경+배지.
  - CSV 내보내기: 기존 `utils/csv.ts`(BOM 포함) 재사용, 현재 필터 결과 기준, 파일명에 출처·날짜.
- 라우팅: `AdminLayout.tsx`에 lazy import + `<Route path="members">`, `AdminSidebar.tsx` 회원 그룹에 "회원 총괄" 메뉴 추가.

## 검증
- `npx tsc -b`: 오류 508건 — **전부 기존 코드의 것(변경 전후 동일 수치로 대조 확인)**, 신규 AdminMembers.tsx·연결 수정분 오류 0건. 이 리포의 빌드 게이트는 `vite build`(package.json build 스크립트에 tsc 없음).
- `npm run build`(vite): 성공. dist → 리포 루트 복사(deploy.cjs 절차 재현: index.html·404.html 교체, assets 구 js/css 제거(images 보존) 후 신규 복사).

## 이유
- 2026-07-18 대표 확정: 회원 DB 통합 운영 + 가입 출처 기록 + www 총괄 관리 체계의 화면 구현.
- DB(www_profiles·가입 트리거·RLS·www_signup_stats·관리자 등록)는 선행 완료 상태였음.

## 남은 일
- 기존 회원 백필분(signup_site null)은 '(미기록)'으로 집계됨 — 사이트별 테이블 대조 추정 채움은 별도 작업.
- 사이트 쪽 재사용 컴포넌트(가입 출처 심기+온보딩)는 04-templates/web에 원본 생성 완료 — 각 사이트 적용은 사이트별 작업 시 진행.
- (기존 부채) tsc 오류 508건은 이 작업 범위 밖 — 정리는 별도 승인 후.
