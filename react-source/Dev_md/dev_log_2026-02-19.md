# 개발일지 - 2026-02-19

**작업 시간**: 2026-02-19 KST
**담당**: Claude Opus 4.6
**프로젝트**: DreamIT Biz React SPA (`D:/www/react-source/`)
**브랜치**: main
**원격 저장소**: https://github.com/aebonlee/www.git

---

## 작업 제목

**Supabase 연동: boardStorage.js Supabase 전용 전환 + DB 테이블 생성 및 시드 데이터 삽입**

---

## 작업 배경

- 기존 `boardStorage.js`는 순수 localStorage 기반으로 게시판/블로그/갤러리 CRUD를 처리
- `supabase.js`(주문 관련)에 이미 `getSupabase()` 패턴이 구현되어 있음
- **localStorage 사용 금지** 지시에 따라 Supabase 전용으로 전환
- Supabase 프로젝트: `hcmgdztsgjvzcyxyayaj` (South Asia - Mumbai)

---

## 작업 내역

### 1. `src/utils/boardStorage.js` — 전체 리팩토링

**변경 전**: 순수 localStorage 기반 (동기 함수 10개)
**변경 후**: Supabase 전용 비동기 함수 10개 (localStorage 완전 제거)

- `getSupabase`를 `supabase.js`에서 import
- 모든 export 함수를 `async`로 전환 (Promise 반환)
- `toCamel(row)` / `toSnake(obj)` 헬퍼 추가 — snake_case ↔ camelCase 자동 변환
- `ensureInitialized()`, `getStore()`, `setStore()`, 시드 데이터 등 localStorage 관련 코드 전면 삭제
- Supabase 미설정 시 빈 배열/null 반환 (에러 안전)

**함수별 Supabase 매핑**:

| 함수 | 테이블 | 쿼리 |
|------|--------|------|
| `getBoardPosts()` | `board_posts` | `.select('*').order('id', {ascending: false})` |
| `getBoardPost(id)` | `board_posts` | `.select('*').eq('id', id).single()` |
| `createBoardPost({...})` | `board_posts` | `.insert({...}).select().single()` |
| `updateBoardPost(id, updates)` | `board_posts` | `.update({...}).eq('id', id).select().single()` |
| `deleteBoardPost(id)` | `board_posts` | `.delete().eq('id', id)` |
| `incrementBoardViews(id)` | `board_posts` | read → views+1 → update |
| `getBlogPosts()` | `blog_posts` | `.select('*').order('id', {ascending: false})` |
| `getBlogPost(id)` | `blog_posts` | `.select('*').eq('id', id).single()` |
| `getGalleryItems()` | `gallery_items` | `.select('*').order('id', {ascending: false})` |
| `getGalleryItem(id)` | `gallery_items` | `.select('*').eq('id', id).single()` |

### 2. 소비 페이지 6개 — async/await 적용

모든 페이지의 boardStorage 함수 호출을 비동기로 전환:

| 파일 | 변경 내용 |
|------|----------|
| `src/pages/Board.jsx` | useEffect 내 async IIFE |
| `src/pages/BoardDetail.jsx` | useEffect async IIFE + handleDelete async |
| `src/pages/BoardWrite.jsx` | useEffect async IIFE + handleSubmit async |
| `src/pages/Blog.jsx` | useEffect async IIFE |
| `src/pages/BlogDetail.jsx` | useEffect async IIFE |
| `src/pages/Gallery.jsx` | useEffect async IIFE |

### 3. Supabase 환경 설정

- `.env` 파일에 Supabase URL 및 Publishable Key 설정
  - `VITE_SUPABASE_URL=https://hcmgdztsgjvzcyxyayaj.supabase.co`
  - `VITE_SUPABASE_ANON_KEY=sb_publishable_*` (새 API Key 형식, supabase-js v2.96.0 호환)
- `.env`는 `.gitignore`에 포함되어 커밋에서 제외

### 4. Supabase DB 테이블 생성 (Management API)

Supabase Management API (`api.supabase.com`)를 통해 원격 실행:

```sql
-- board_posts (게시판)
CREATE TABLE board_posts (
  id BIGSERIAL PRIMARY KEY,
  category TEXT NOT NULL DEFAULT 'free',
  title TEXT NOT NULL,
  content TEXT NOT NULL,
  author TEXT NOT NULL DEFAULT '익명',
  date TEXT,
  views INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

-- blog_posts (블로그)
CREATE TABLE blog_posts (
  id BIGSERIAL PRIMARY KEY,
  category TEXT, category_en TEXT,
  title TEXT NOT NULL, title_en TEXT,
  excerpt TEXT, excerpt_en TEXT,
  content TEXT, content_en TEXT,
  icon TEXT DEFAULT '📝',
  date TEXT,
  created_at TIMESTAMPTZ DEFAULT now()
);

-- gallery_items (갤러리)
CREATE TABLE gallery_items (
  id BIGSERIAL PRIMARY KEY,
  title TEXT NOT NULL, title_en TEXT,
  category TEXT DEFAULT 'project',
  image_url TEXT,
  description TEXT, description_en TEXT,
  date TEXT,
  created_at TIMESTAMPTZ DEFAULT now()
);
```

### 5. RLS (Row Level Security) 정책 설정

| 테이블 | 정책 |
|--------|------|
| `board_posts` | SELECT/INSERT/UPDATE/DELETE 모두 public 허용 |
| `blog_posts` | SELECT만 public 허용 |
| `gallery_items` | SELECT만 public 허용 |

### 6. 시드 데이터 삽입

| 테이블 | 건수 |
|--------|------|
| `board_posts` | 5건 (공지 2, 자유 2, Q&A 1) |
| `blog_posts` | 6건 (교육, 컨설팅, 호스팅, 디자인, 기술, IT트렌드) |
| `gallery_items` | 8건 (프로젝트 3, 교육 2, 이벤트 2, 사무실 1) |

### 7. 문서화

- `Dev_md/supabase_board_integration.md` — Supabase 연동 기술 문서 작성

---

## 수정 파일 목록

| # | 파일 | 변경 유형 |
|---|------|----------|
| 1 | `src/utils/boardStorage.js` | 전체 리팩토링 |
| 2 | `src/pages/Board.jsx` | async 적용 |
| 3 | `src/pages/BoardDetail.jsx` | async 적용 |
| 4 | `src/pages/BoardWrite.jsx` | async 적용 |
| 5 | `src/pages/Blog.jsx` | async 적용 |
| 6 | `src/pages/BlogDetail.jsx` | async 적용 |
| 7 | `src/pages/Gallery.jsx` | async 적용 |
| 8 | `Dev_md/supabase_board_integration.md` | 신규 |
| 9 | `Dev_md/dev_log_2026-02-19.md` | 신규 |

---

## 컬럼명 매핑 규칙

`toCamel(row)` / `toSnake(obj)` 헬퍼로 자동 변환:

| Supabase (snake_case) | JS (camelCase) |
|------------------------|----------------|
| `category_en` | `categoryEn` |
| `title_en` | `titleEn` |
| `excerpt_en` | `excerptEn` |
| `content_en` | `contentEn` |
| `image_url` | `imageUrl` |
| `description_en` | `descriptionEn` |

---

## 빌드 결과

- `npm run build` — 성공 (vite v7.3.1, 2.14s)
- `npm run dev` — 정상 기동 (HTTP 200, localhost:5173)

---

## 설계 원칙

- **Supabase 전용**: localStorage 사용 안 함
- **에러 안전**: Supabase 쿼리 실패 시 빈 배열/null 반환 + console.error
- **하위 호환**: 기존 함수명/파라미터 동일, 반환값만 Promise로 변경
- **컬럼명 매핑**: 자동 snake_case ↔ camelCase 변환으로 소비 페이지 변경 최소화

---
---

# 세션 2: 사이트 전체 점검 — 파비콘 + 간격 + 타이포그래피 최적화

**작업 시간**: 2026-02-19 KST
**담당**: Claude Opus 4.6
**커밋**: `0ee2bf1`

---

## 작업 배경

- CSS 16개 파일 전수 감사 결과 다수 개선 사항 발견
- 파비콘이 Vite 기본 로고(`vite.svg`) 상태로 브랜드 아이덴티티 부재
- 섹션 간격·그리드 갭이 불규칙 (60px, 80px, 100px 혼재)
- 한글 단어 끊김 방지(`word-break`)가 거의 적용되지 않음
- 자간(`letter-spacing`)과 행간(`line-height`)이 일부 요소에만 설정됨
- 포트폴리오 이미지 표시 위치 및 텍스트 줄바꿈 개선 필요

---

## 작업 내역

### A1. 파비콘 교체

| 항목 | 내용 |
|------|------|
| `public/favicon.svg` | 신규 생성 — "D" 이니셜 + 브랜드 블루(#0046C8) 라운드 아이콘 |
| `index.html` | `href="/vite.svg"` → `href="/favicon.svg"` |

### B. 한글 가독성 (word-break)

| 항목 | 파일 | 변경 |
|------|------|------|
| B1 | `base.css` body | `word-break: keep-all; overflow-wrap: break-word;` 추가 |
| B2 | `base.css` h1~h4 | `word-break: keep-all; overflow-wrap: break-word;` 신규 룰 |

### C. 행간(line-height) 표준화

| 항목 | 파일 | 수정 전 | 수정 후 |
|------|------|---------|---------|
| C1 제목 행간 | `base.css` | body 1.7 상속 | h1:1.3 / h2:1.35 / h3:1.4 / h4:1.45 |
| C2 블로그 h3 | `blog.css` | 1.4 | 1.5 |
| C2 블로그 h2 | `blog.css` | 1.4 | 1.45 |
| C3 본문 행간 | `community.css` `.blog-detail-content` | 1.9 | 1.8 |

### D. 자간(letter-spacing) 보강

| 항목 | 파일 | 수정 전 | 수정 후 |
|------|------|---------|---------|
| D1 본문 자간 | `base.css` body | 없음 | `-0.01em` |
| D2 소제목 자간 | `base.css` h3 | 없음 | `-0.015em` |
| D2 소제목 자간 | `base.css` h4 | 없음 | `-0.01em` |

### E. 섹션 간격(padding) 통일

| 항목 | 파일 | 수정 전 | 수정 후 | 비고 |
|------|------|---------|---------|------|
| E1 변수 | `base.css` `--section-padding` | 100px | 80px | 현재 미참조, 향후 대비 |
| E2 히어로 | `hero.css` `.hero` padding-bottom | 60px | 80px | |
| E3 푸터 | `footer.css` `.footer` padding | 60px 0 24px | 80px 0 32px | |

### F. 그리드 갭(gap) 정리

| 항목 | 파일 | 셀렉터 | 수정 전 | 수정 후 |
|------|------|--------|---------|---------|
| F1 소형 | `community.css` | `.gallery-grid` | 20px | 24px |
| F1 소형 | `ceo.css` | `.ceo-cert-grid` | 16px | 24px |
| F2 대형 | `about.css` | `.about-wrapper` | 80px | 60px |
| F2 대형 | `portfolio.css` | `.project-content` | 60px | 48px |
| F2 대형 | `contact.css` | `.contact-grid` | 60px | 48px |
| F2 대형 | `footer.css` | `.footer-content` | 60px | 48px |

### G. 기타 미세 조정

| 항목 | 파일 | 셀렉터 | 수정 전 | 수정 후 |
|------|------|--------|---------|---------|
| G1 | `shop.css` | `.product-info` padding | 20px | 24px |
| G2 | `community.css` | `.pagination` gap | 4px | 6px |

### 포트폴리오 이미지/텍스트 개선

| 파일 | 변경 |
|------|------|
| `Home.jsx` | 세종대 이미지에 `objectPosition: 'top'` 추가 (상단 기준 표시) |
| `Portfolio.jsx` | 세종대 이미지에 `objectPosition: 'top'` 추가 |
| `Portfolio.jsx` | 진주교대 제목 `<br />` 줄바꿈 (2줄 분리) |

---

## 수정 파일 목록

| # | 파일 | 변경 유형 |
|---|------|----------|
| 1 | `public/favicon.svg` | 신규 생성 |
| 2 | `index.html` | 파비콘 경로 수정 |
| 3 | `src/styles/base.css` | B1, B2, C1, D1, D2, E1 |
| 4 | `src/styles/hero.css` | E2 |
| 5 | `src/styles/footer.css` | E3, F2 |
| 6 | `src/styles/blog.css` | C2 |
| 7 | `src/styles/community.css` | C3, F1, G2 |
| 8 | `src/styles/about.css` | F2 |
| 9 | `src/styles/portfolio.css` | F2 |
| 10 | `src/styles/contact.css` | F2 |
| 11 | `src/styles/ceo.css` | F1 |
| 12 | `src/styles/shop.css` | G1 |
| 13 | `src/pages/Home.jsx` | 세종대 이미지 상단 정렬 |
| 14 | `src/pages/Portfolio.jsx` | 세종대 이미지 상단 정렬, 진주교대 제목 2줄 |
| 15 | `Dev_md/site_optimization_checklist.md` | 점검표 문서 신규 |

---

## 의사결정 기록

사용자 선택이 필요했던 3개 항목:

| 항목 | 선택지 | 사용자 결정 |
|------|--------|------------|
| E1 `--section-padding` 변수 (현재 미사용) | A: 80px 변경 / B: 스킵 | **A: 80px 변경** (향후 대비) |
| C2 블로그 h3 행간 (line-clamp 2 영향) | 1.5 변경 / 1.4 유지 | **1.5 변경** |
| D1 body 전역 자간 | A: body -0.01em / B: p,li,td만 / C: 스킵 | **A: body -0.01em** |

---

## 빌드 결과

- `npm run build` — 성공 (vite v7.3.1, 3.63s)
- 커밋 `0ee2bf1` → `git push` 완료

---

## 설계 원칙

- **최소 변경**: 점검표 전 항목을 사용자 확인 후 적용, 무분별한 수정 방지
- **특이성 존중**: base.css의 글로벌 룰은 컴포넌트별 CSS에서 이미 오버라이드되는 값은 건드리지 않음
- **문서화 우선**: 수정 전 `site_optimization_checklist.md` 저장 후 작업 진행

---
---

# 세션 3: GitHub Pages 배포 구조 수정 — 라이브 사이트 반영 문제 해결

**작업 시간**: 2026-02-19 KST
**담당**: Claude Opus 4.6
**커밋**: `8a741f1`

---

## 문제 현상

세션 2에서 수행한 모든 변경(파비콘, 포트폴리오 이미지, 텍스트 줄바꿈)이 라이브 사이트(`www.dreamitbiz.com`)에 반영되지 않음:

- 파비콘: 여전히 React/Vite 기본 로고 표시
- 포트폴리오 세종대 이미지: 상단 정렬 적용 안 됨 (여전히 중앙)
- 진주교대 제목: 2줄 분리 적용 안 됨

---

## 원인 분석

### 핵심 발견: GitHub Pages 서빙 경로 불일치

| 항목 | 실제 상태 |
|------|----------|
| Git 저장소 루트 | `D:\www/` |
| React 소스 | `D:\www\react-source/` |
| Vite 빌드 출력 | `D:\www\react-source\dist/` |
| GitHub Pages 서빙 위치 | `D:\www/` (루트) |
| CNAME 파일 | `D:\www\CNAME` → `www.dreamitbiz.com` |

**문제**: `npm run build`는 `react-source/dist/`에 결과물을 생성하지만, GitHub Pages는 저장소 루트(`D:\www/`)에서 파일을 서빙함. 루트의 `index.html`은 **구버전 빌드 해시**(`index-VBzfrMeh.js`)를 참조하고 있었음.

### 루트 디렉토리 오염 상태

루트 `assets/` 폴더에 **4세대 이상의 오래된 빌드 해시 파일**이 축적되어 있었음:

```
assets/index-VBzfrMeh.js     (루트 index.html이 참조하던 구버전)
assets/index-DGU5HXge.js     (더 오래된 버전)
assets/index-CJ8ayIS6.js     (최신 빌드 - 있어야 할 파일)
assets/index-gFniYBP6.css    (최신 빌드 - 있어야 할 파일)
... 등 다수
```

### 비교: 루트 index.html vs 빌드 index.html

| 파일 | JS 참조 | 파비콘 |
|------|---------|--------|
| `D:\www\index.html` (배포용) | `index-VBzfrMeh.js` (구버전) | `vite.svg` |
| `D:\www\react-source\dist\index.html` (최신 빌드) | `index-CJ8ayIS6.js` (신버전) | `favicon.svg` |

---

## 수정 작업

### 1단계: 루트 오래된 파일 정리

```bash
git -C D:/www rm -rf assets/          # 오래된 4세대 빌드 파일 일괄 삭제
git -C D:/www rm index.html           # 구버전 index.html 삭제
git -C D:/www rm vite.svg             # Vite 기본 파비콘 삭제
git -C D:/www rm 404.html             # 구버전 404 삭제
```

- 삭제된 파일: **151개** (누적된 구 빌드 에셋)

### 2단계: 최신 빌드 결과물 루트로 복사

```bash
cp -r D:/www/react-source/dist/assets D:/www/    # 빌드된 assets/ 복사
cp D:/www/react-source/dist/index.html D:/www/   # 최신 index.html 복사
cp D:/www/react-source/dist/favicon.svg D:/www/  # 신규 파비콘 복사
cp D:/www/react-source/dist/404.html D:/www/     # 404 페이지 복사
```

### 3단계: 커밋 & 푸시

```bash
git -C D:/www add assets/ index.html favicon.svg 404.html
git -C D:/www commit -m "deploy: 최신 빌드를 루트로 배포 + 구 에셋 정리"
git -C D:/www push origin main
```

- 커밋 `8a741f1` — 151 files changed

---

## 배포 구조 정리

### Before (문제 상태)

```
D:\www/                     ← GitHub Pages 서빙 루트
├── index.html              ← 구버전 (index-VBzfrMeh.js 참조)
├── vite.svg                ← Vite 기본 파비콘
├── assets/                 ← 4세대 빌드 파일 혼재
│   ├── index-VBzfrMeh.js   (루트 HTML이 참조)
│   ├── index-DGU5HXge.js   (미사용)
│   ├── index-CJ8ayIS6.js   (최신이지만 참조 안 됨)
│   └── ... 다수 구 파일
├── CNAME
└── react-source/
    └── dist/               ← 최신 빌드 (여기만 업데이트됨)
        ├── index.html      ← 신버전 (index-CJ8ayIS6.js 참조, favicon.svg)
        └── assets/
```

### After (정상 상태)

```
D:\www/                     ← GitHub Pages 서빙 루트
├── index.html              ← 최신 빌드 (index-CJ8ayIS6.js 참조, favicon.svg)
├── favicon.svg             ← DreamIT Biz 브랜드 파비콘
├── 404.html                ← SPA 라우팅 폴백
├── assets/                 ← 최신 빌드만 포함
│   ├── index-CJ8ayIS6.js
│   ├── index-gFniYBP6.css
│   └── images/portfolio/   ← 포트폴리오 이미지
├── CNAME
└── react-source/           ← 소스 코드
    └── dist/               ← 빌드 출력 (루트와 동일)
```

---

## 향후 배포 프로세스 (교훈)

빌드 후 라이브 반영까지의 **필수 단계**:

```
1. cd react-source && npm run build      # Vite 빌드
2. 루트 assets/ 삭제                      # 이전 빌드 정리
3. react-source/dist/* → D:\www/ 복사     # 최신 빌드 배포
4. git add & commit & push               # GitHub Pages 반영
```

> `npm run build`만으로는 라이브 사이트에 반영되지 않음.
> 반드시 `dist/` → 루트 복사 + git push 필요.

---

## 기술 노트

- **Windows bash 호환성 이슈**: `cd "D:\www" && git status 2>&1 | head -20` 형태의 파이프 명령이 exit code 1 반환. `git -C D:/www` 문법으로 우회 해결.
- **Git 추적 상태**: 로컬에서 삭제된 파일이 Git에서는 여전히 추적 중 (Working tree에 `D` 표시). `git checkout HEAD --`로 복원 후 `git rm -rf`로 정리 필요.

---
---

# 세션 4: 종합 기능 개발 — 인증 + 블로그/갤러리/스토어 CRUD + 주문이력

**작업 시간**: 2026-02-19 KST
**담당**: Claude Opus 4.6
**커밋**: `40eb110`

---

## 작업 배경

- 인증 시스템이 전무한 상태 — 모든 페이지가 익명 접근
- 블로그/갤러리는 읽기 전용 (Supabase에서 로딩만 가능, 쓰기 없음)
- 스토어 상품 22개가 하드코딩 JS 파일(`publishingDetails.js`, `educationDetails.js`)에 정의
- 게시판은 누구나 글쓰기/수정/삭제 가능 (인증 없음)
- 주문 시 `user_id` 없이 저장 → 마이페이지에서 주문 이력 조회 불가

---

## 작업 내역 (6 Phase)

### Phase 0: 인증 시스템 (Supabase Auth + 소셜 로그인)

**신규 테이블**: `user_profiles`
- `id` (UUID, PK, auth.users FK), `email`, `display_name`, `avatar_url`, `role` ('user'|'admin'), `provider`
- RLS: 누구나 SELECT, 본인만 UPDATE
- 트리거: `on_auth_user_created` → 회원가입 시 자동 프로필 생성

**신규 파일 8개**:

| # | 파일 | 용도 |
|---|------|------|
| 1 | `src/contexts/AuthContext.jsx` | 세션 관리, `onAuthStateChange` 리스너, `isAdmin` 판별 |
| 2 | `src/utils/auth.js` | `signInWithGoogle()`, `signInWithKakao()`, `signInWithNaver()`, `signInWithEmail()`, `signUp()`, `signOut()`, `getProfile()`, `updateProfile()` |
| 3 | `src/components/AuthGuard.jsx` | 인증 필요 라우트 래퍼 (미로그인 → `/login` 리다이렉트) |
| 4 | `src/components/AdminGuard.jsx` | 관리자 전용 라우트 래퍼 |
| 5 | `src/pages/Login.jsx` | 소셜 로그인 버튼 3종(Google/Kakao/Naver) + 이메일/비밀번호 폼 |
| 6 | `src/pages/Register.jsx` | 이메일 회원가입 폼 (이름, 이메일, 비밀번호, 확인) |
| 7 | `src/pages/MyPage.jsx` | 프로필 조회/수정, 아바타, 관리자 뱃지, 주문 이력 링크 |
| 8 | `src/styles/auth.css` | 로그인/가입/마이페이지 + Navbar 유저 메뉴 스타일 |

**수정 파일**:

| 파일 | 변경 |
|------|------|
| `src/App.jsx` | `<AuthProvider>` 래핑, `/login` `/register` `/mypage` 라우트 추가 |
| `src/components/layout/Navbar.jsx` | 로그인 버튼 or 유저 아바타+드롭다운 메뉴 (마이페이지, 주문이력, 로그아웃) |
| `src/utils/translations.js` | `auth.*` 번역 키 40개+ 추가 (ko/en) |

---

### Phase 1: 블로그 CRUD (관리자 전용)

**DB 변경**: `blog_posts` 테이블에 `author_id`, `image_url`, `updated_at` 컬럼 추가
**RLS**: 관리자만 INSERT/UPDATE/DELETE

**신규**: `src/pages/BlogWrite.jsx` — 블로그 글쓰기/수정 폼 (카테고리, 제목(ko/en), 요약, 본문, 아이콘, 이미지URL)

**수정**:

| 파일 | 변경 |
|------|------|
| `src/utils/boardStorage.js` | `createBlogPost()`, `updateBlogPost()`, `deleteBlogPost()` 추가 |
| `src/pages/Blog.jsx` | 관리자에게 "글쓰기" 버튼 표시 |
| `src/pages/BlogDetail.jsx` | 관리자에게 수정/삭제 버튼 + 실제 이미지 표시 |
| `src/App.jsx` | `/community/blog/write`, `/community/blog/edit/:id` 라우트 (AdminGuard) |

---

### Phase 2: 갤러리 CRUD + 이미지 업로드

**DB 변경**: `gallery_items`에 `author_id`, `updated_at` 추가
**Storage**: `media` 버킷 생성 (public read, admin-only write/delete)

**신규 3개**:

| # | 파일 | 용도 |
|---|------|------|
| 1 | `src/pages/GalleryWrite.jsx` | 갤러리 아이템 등록/수정 + 이미지 업로드 |
| 2 | `src/components/ImageUpload.jsx` | 드래그앤드롭 이미지 업로드 컴포넌트 (미리보기, 삭제) |
| 3 | `src/utils/storage.js` | `uploadImage(file, folder)`, `deleteImage(publicUrl)` |

**수정**:

| 파일 | 변경 |
|------|------|
| `src/utils/boardStorage.js` | `createGalleryItem()`, `updateGalleryItem()`, `deleteGalleryItem()` 추가 |
| `src/pages/Gallery.jsx` | 관리자 "업로드" 버튼, 실제 이미지 표시 (그라데이션 폴백 유지) |

---

### Phase 3: 스토어 DB 전환 + 상품 관리

**신규 테이블**: `products`
- `id` (SERIAL), `slug` (UNIQUE), `category`, `title`/`title_en`, `description`/`description_en`, `price`, `image_url`, `is_sold_out`, `is_active`, `sort_order`
- RLS: 누구나 SELECT, 관리자만 INSERT/UPDATE/DELETE
- 기존 하드코딩 22개 상품 → INSERT SQL로 마이그레이션

**신규 2개**:

| # | 파일 | 용도 |
|---|------|------|
| 1 | `src/utils/productStorage.js` | `getProducts()`, `getProduct()`, `createProduct()`, `updateProduct()`, `deleteProduct()`, `toggleSoldOut()` — Supabase 미설정 시 하드코딩 폴백 |
| 2 | `src/pages/ProductWrite.jsx` | 관리자 상품 등록/수정 폼 + ImageUpload |

**수정**:

| 파일 | 변경 |
|------|------|
| `src/pages/Shop.jsx` | 하드코딩 import 제거 → `getProducts()`로 DB 로딩, 판매완료 뱃지, 관리자 상품 관리 버튼 |
| `src/contexts/CartContext.jsx` | `addItem()`에 `isSoldOut` 체크 추가 |
| `src/styles/shop.css` | `.sold-out-badge`, `.product-admin-actions`, 이미지 업로드 스타일 추가 |

---

### Phase 4: 게시판 인증 연동

**DB 변경**: `board_posts`에 `author_id` 추가
**RLS 재설정**: SELECT=공개, INSERT=로그인필수, UPDATE/DELETE=본인 또는 관리자

**수정**:

| 파일 | 변경 |
|------|------|
| `src/pages/Board.jsx` | 로그인 시만 "글쓰기" 버튼 표시 |
| `src/pages/BoardWrite.jsx` | 작성자 자동 입력 (profile.display_name), `author_id` 저장, 미로그인 시 `/login` 리다이렉트 |
| `src/pages/BoardDetail.jsx` | 본인 글 또는 관리자만 수정/삭제 버튼 표시 |
| `src/utils/boardStorage.js` | `createBoardPost`에 `author_id` 포함 |

---

### Phase 5: 주문 이력 + 마이페이지 보강

**DB 변경**: `orders`에 `user_id` 추가, 사용자별 조회 RLS

**신규**: `src/pages/OrderHistory.jsx` — 내 주문 목록 (주문번호, 상태 뱃지, 날짜, 금액, 상세 품목)

**수정**:

| 파일 | 변경 |
|------|------|
| `src/pages/Checkout.jsx` | 로그인 시 이름/이메일 자동 입력, `user_id` 주문에 포함 |
| `src/utils/supabase.js` | `getOrdersByUser(userId)` 함수 추가 |
| `src/pages/MyPage.jsx` | 주문 이력 링크 섹션 |
| `src/App.jsx` | `/mypage/orders` 라우트 (AuthGuard) |

---

### Supabase 마이그레이션 SQL

`supabase_migration.sql` 파일 신규 생성 — 전체 Phase의 DDL을 단일 파일로 통합:

| 유형 | 내용 |
|------|------|
| 신규 테이블 | `user_profiles`, `products` |
| 신규 Storage | `media` 버킷 (public) |
| 컬럼 추가 | `blog_posts` (author_id, image_url, updated_at) |
| 컬럼 추가 | `gallery_items` (author_id, updated_at) |
| 컬럼 추가 | `board_posts` (author_id) |
| 컬럼 추가 | `orders` (user_id) |
| RLS 정책 | ~15개 신규 |
| 트리거 | `on_auth_user_created` → 프로필 자동 생성 |
| 시드 데이터 | `products` 22건 (books, ebooks, periodicals, courses) |

---

## 수정 파일 전체 목록

### 신규 파일 (15 + 1 SQL)

| # | 파일 | Phase |
|---|------|-------|
| 1 | `src/contexts/AuthContext.jsx` | 0 |
| 2 | `src/utils/auth.js` | 0 |
| 3 | `src/components/AuthGuard.jsx` | 0 |
| 4 | `src/components/AdminGuard.jsx` | 0 |
| 5 | `src/pages/Login.jsx` | 0 |
| 6 | `src/pages/Register.jsx` | 0 |
| 7 | `src/pages/MyPage.jsx` | 0 |
| 8 | `src/styles/auth.css` | 0 |
| 9 | `src/pages/BlogWrite.jsx` | 1 |
| 10 | `src/pages/GalleryWrite.jsx` | 2 |
| 11 | `src/components/ImageUpload.jsx` | 2 |
| 12 | `src/utils/storage.js` | 2 |
| 13 | `src/utils/productStorage.js` | 3 |
| 14 | `src/pages/ProductWrite.jsx` | 3 |
| 15 | `src/pages/OrderHistory.jsx` | 5 |
| 16 | `supabase_migration.sql` | 전체 |

### 수정 파일 (15개)

| # | 파일 | Phase |
|---|------|-------|
| 1 | `src/App.jsx` | 0,1,2,3,4,5 |
| 2 | `src/components/layout/Navbar.jsx` | 0 |
| 3 | `src/utils/boardStorage.js` | 1,2,4 |
| 4 | `src/utils/supabase.js` | 5 |
| 5 | `src/utils/translations.js` | 0~5 |
| 6 | `src/pages/Blog.jsx` | 1 |
| 7 | `src/pages/BlogDetail.jsx` | 1 |
| 8 | `src/pages/Gallery.jsx` | 2 |
| 9 | `src/pages/Shop.jsx` | 3 |
| 10 | `src/contexts/CartContext.jsx` | 3 |
| 11 | `src/pages/Board.jsx` | 4 |
| 12 | `src/pages/BoardDetail.jsx` | 4 |
| 13 | `src/pages/BoardWrite.jsx` | 4 |
| 14 | `src/pages/Checkout.jsx` | 5 |
| 15 | `src/styles/shop.css` | 3,5 |

---

## 빌드 결과

- `npm run build` — 성공 (vite v7.3.1, 2.71s, 147 modules)
- 에러/경고: 0건
- 커밋: `40eb110` (92 files changed, +3385, -212)
- `git push origin main` — 성공
- `dist/` → `D:\www/` 루트 복사 배포 완료

---

## 사용자 수동 조치 필요 사항

아래 항목은 외부 서비스 콘솔에서 직접 설정 필요:

1. **Supabase SQL Editor**: `supabase_migration.sql` 실행 (테이블, RLS, 트리거, 시드 데이터)
2. **Google Cloud Console**: OAuth 2.0 클라이언트 생성 → Client ID/Secret 획득
3. **Kakao Developers**: 앱 등록 → REST API Key + Client Secret 획득
4. **Naver Developers**: 앱 등록 → Client ID/Secret 획득
5. **Supabase Dashboard**: Auth > Providers에서 Google/Kakao/Naver 활성화 + 키 입력
6. **최초 관리자**: 회원가입 후 `user_profiles.role = 'admin'` 수동 설정

---

## 설계 원칙

- **점진적 강화**: Supabase 미설정 시 기존 기능 유지 (하드코딩 폴백, localStorage 폴백)
- **역할 기반 접근**: RLS + 프론트엔드 Guard 이중 보안
- **기존 패턴 준수**: snake_case ↔ camelCase 변환, lazy loading, AOS 애니메이션
- **비로그인 허용**: 읽기 + 스토어 결제는 로그인 없이도 가능

---
---

# 세션 5: Supabase 마이그레이션 실행 + OAuth 프로바이더 설정 + 네이버 제거

**작업 시간**: 2026-02-19 KST
**담당**: Claude Opus 4.6

---

## 작업 내역

### 1. Supabase 마이그레이션 SQL 원격 실행

Supabase Management API (`api.supabase.com`)를 통해 `supabase_migration.sql` 전체를 Phase별로 원격 실행:

| Phase | 내용 | 결과 |
|-------|------|------|
| 0 | `user_profiles` 테이블 + RLS + 자동 프로필 트리거 | 성공 |
| 1 | `blog_posts` 컬럼 추가 + 관리자 전용 RLS 4개 | 성공 |
| 2a | `gallery_items` 컬럼 추가 + 관리자 전용 RLS 4개 | 성공 |
| 2b | `media` Storage 버킷 + 공개 읽기/관리자 쓰기 정책 | 성공 |
| 3a | `products` 테이블 + RLS 4개 | 성공 |
| 3b | 상품 22건 시드 데이터 INSERT | 성공 |
| 4 | `board_posts` author_id + 인증 기반 RLS 재설정 | 성공 |
| 5a | `orders` + `order_items` 테이블 신규 생성 + RLS | 성공 |
| 5b | 주문 관련 RLS 정책 (사용자별 조회, 비로그인 삽입 허용) | 성공 |

**최종 DB 상태:**
- 테이블 7개: `blog_posts`, `board_posts`, `gallery_items`, `order_items`, `orders`, `products`, `user_profiles`
- RLS 정책 22개
- 트리거 1개: `on_auth_user_created`
- Storage 버킷: `media` (public)
- 상품 데이터: 22건

### 2. Supabase Auth 설정

**site_url 업데이트:**
- `http://localhost:3000` → `https://www.dreamitbiz.com`
- Redirect 허용: `www.dreamitbiz.com/**`, `localhost:5173/**`

**OAuth 프로바이더 활성화:**

| 프로바이더 | 상태 | Client ID |
|-----------|------|-----------|
| Google | 활성화 | `899974314812-...apps.googleusercontent.com` |
| Kakao | 활성화 | `c71805c94a02e91c2c1ea4ff50c0d159` |
| 이메일 | 활성화 | (기본) |

### 3. 네이버 로그인 제거

Supabase가 네이버를 네이티브 지원하지 않아 제거 결정:

| 파일 | 변경 |
|------|------|
| `src/pages/Login.jsx` | 네이버 소셜 버튼 + import 제거, `handleSocialLogin`에서 naver 분기 제거 |
| `src/utils/auth.js` | `signInWithNaver()` 함수 전체 삭제 |

---

## 수정 파일 목록

| # | 파일 | 변경 유형 |
|---|------|----------|
| 1 | `src/pages/Login.jsx` | 네이버 버튼/import 제거 |
| 2 | `src/utils/auth.js` | `signInWithNaver()` 삭제 |

---

## 빌드 결과

- `npm run build` — 성공 (vite v7.3.1, 5.31s, 147 modules)
- 에러/경고: 0건
- `dist/` → `D:\www/` 루트 복사 배포 완료
