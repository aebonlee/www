# 개발일지 - 2026-02-19

**작업 시간**: 2026-02-19 KST
**담당**: Claude Opus 4.6
**프로젝트**: DreamIT Biz React SPA (`D:/www/react-source/`)
**브랜치**: main
**원격 저장소**: https://github.com/aebonlee/www.git

---

## 작업 제목

**Supabase 연동: boardStorage.js Supabase 전용 전환 + DB 테이블 생성 및 시드 데이터 삽입**

---

## 작업 배경

- 기존 `boardStorage.js`는 순수 localStorage 기반으로 게시판/블로그/갤러리 CRUD를 처리
- `supabase.js`(주문 관련)에 이미 `getSupabase()` 패턴이 구현되어 있음
- **localStorage 사용 금지** 지시에 따라 Supabase 전용으로 전환
- Supabase 프로젝트: `hcmgdztsgjvzcyxyayaj` (South Asia - Mumbai)

---

## 작업 내역

### 1. `src/utils/boardStorage.js` — 전체 리팩토링

**변경 전**: 순수 localStorage 기반 (동기 함수 10개)
**변경 후**: Supabase 전용 비동기 함수 10개 (localStorage 완전 제거)

- `getSupabase`를 `supabase.js`에서 import
- 모든 export 함수를 `async`로 전환 (Promise 반환)
- `toCamel(row)` / `toSnake(obj)` 헬퍼 추가 — snake_case ↔ camelCase 자동 변환
- `ensureInitialized()`, `getStore()`, `setStore()`, 시드 데이터 등 localStorage 관련 코드 전면 삭제
- Supabase 미설정 시 빈 배열/null 반환 (에러 안전)

**함수별 Supabase 매핑**:

| 함수 | 테이블 | 쿼리 |
|------|--------|------|
| `getBoardPosts()` | `board_posts` | `.select('*').order('id', {ascending: false})` |
| `getBoardPost(id)` | `board_posts` | `.select('*').eq('id', id).single()` |
| `createBoardPost({...})` | `board_posts` | `.insert({...}).select().single()` |
| `updateBoardPost(id, updates)` | `board_posts` | `.update({...}).eq('id', id).select().single()` |
| `deleteBoardPost(id)` | `board_posts` | `.delete().eq('id', id)` |
| `incrementBoardViews(id)` | `board_posts` | read → views+1 → update |
| `getBlogPosts()` | `blog_posts` | `.select('*').order('id', {ascending: false})` |
| `getBlogPost(id)` | `blog_posts` | `.select('*').eq('id', id).single()` |
| `getGalleryItems()` | `gallery_items` | `.select('*').order('id', {ascending: false})` |
| `getGalleryItem(id)` | `gallery_items` | `.select('*').eq('id', id).single()` |

### 2. 소비 페이지 6개 — async/await 적용

모든 페이지의 boardStorage 함수 호출을 비동기로 전환:

| 파일 | 변경 내용 |
|------|----------|
| `src/pages/Board.jsx` | useEffect 내 async IIFE |
| `src/pages/BoardDetail.jsx` | useEffect async IIFE + handleDelete async |
| `src/pages/BoardWrite.jsx` | useEffect async IIFE + handleSubmit async |
| `src/pages/Blog.jsx` | useEffect async IIFE |
| `src/pages/BlogDetail.jsx` | useEffect async IIFE |
| `src/pages/Gallery.jsx` | useEffect async IIFE |

### 3. Supabase 환경 설정

- `.env` 파일에 Supabase URL 및 Publishable Key 설정
  - `VITE_SUPABASE_URL=https://hcmgdztsgjvzcyxyayaj.supabase.co`
  - `VITE_SUPABASE_ANON_KEY=sb_publishable_*` (새 API Key 형식, supabase-js v2.96.0 호환)
- `.env`는 `.gitignore`에 포함되어 커밋에서 제외

### 4. Supabase DB 테이블 생성 (Management API)

Supabase Management API (`api.supabase.com`)를 통해 원격 실행:

```sql
-- board_posts (게시판)
CREATE TABLE board_posts (
  id BIGSERIAL PRIMARY KEY,
  category TEXT NOT NULL DEFAULT 'free',
  title TEXT NOT NULL,
  content TEXT NOT NULL,
  author TEXT NOT NULL DEFAULT '익명',
  date TEXT,
  views INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

-- blog_posts (블로그)
CREATE TABLE blog_posts (
  id BIGSERIAL PRIMARY KEY,
  category TEXT, category_en TEXT,
  title TEXT NOT NULL, title_en TEXT,
  excerpt TEXT, excerpt_en TEXT,
  content TEXT, content_en TEXT,
  icon TEXT DEFAULT '📝',
  date TEXT,
  created_at TIMESTAMPTZ DEFAULT now()
);

-- gallery_items (갤러리)
CREATE TABLE gallery_items (
  id BIGSERIAL PRIMARY KEY,
  title TEXT NOT NULL, title_en TEXT,
  category TEXT DEFAULT 'project',
  image_url TEXT,
  description TEXT, description_en TEXT,
  date TEXT,
  created_at TIMESTAMPTZ DEFAULT now()
);
```

### 5. RLS (Row Level Security) 정책 설정

| 테이블 | 정책 |
|--------|------|
| `board_posts` | SELECT/INSERT/UPDATE/DELETE 모두 public 허용 |
| `blog_posts` | SELECT만 public 허용 |
| `gallery_items` | SELECT만 public 허용 |

### 6. 시드 데이터 삽입

| 테이블 | 건수 |
|--------|------|
| `board_posts` | 5건 (공지 2, 자유 2, Q&A 1) |
| `blog_posts` | 6건 (교육, 컨설팅, 호스팅, 디자인, 기술, IT트렌드) |
| `gallery_items` | 8건 (프로젝트 3, 교육 2, 이벤트 2, 사무실 1) |

### 7. 문서화

- `Dev_md/supabase_board_integration.md` — Supabase 연동 기술 문서 작성

---

## 수정 파일 목록

| # | 파일 | 변경 유형 |
|---|------|----------|
| 1 | `src/utils/boardStorage.js` | 전체 리팩토링 |
| 2 | `src/pages/Board.jsx` | async 적용 |
| 3 | `src/pages/BoardDetail.jsx` | async 적용 |
| 4 | `src/pages/BoardWrite.jsx` | async 적용 |
| 5 | `src/pages/Blog.jsx` | async 적용 |
| 6 | `src/pages/BlogDetail.jsx` | async 적용 |
| 7 | `src/pages/Gallery.jsx` | async 적용 |
| 8 | `Dev_md/supabase_board_integration.md` | 신규 |
| 9 | `Dev_md/dev_log_2026-02-19.md` | 신규 |

---

## 컬럼명 매핑 규칙

`toCamel(row)` / `toSnake(obj)` 헬퍼로 자동 변환:

| Supabase (snake_case) | JS (camelCase) |
|------------------------|----------------|
| `category_en` | `categoryEn` |
| `title_en` | `titleEn` |
| `excerpt_en` | `excerptEn` |
| `content_en` | `contentEn` |
| `image_url` | `imageUrl` |
| `description_en` | `descriptionEn` |

---

## 빌드 결과

- `npm run build` — 성공 (vite v7.3.1, 2.14s)
- `npm run dev` — 정상 기동 (HTTP 200, localhost:5173)

---

## 설계 원칙

- **Supabase 전용**: localStorage 사용 안 함
- **에러 안전**: Supabase 쿼리 실패 시 빈 배열/null 반환 + console.error
- **하위 호환**: 기존 함수명/파라미터 동일, 반환값만 Promise로 변경
- **컬럼명 매핑**: 자동 snake_case ↔ camelCase 변환으로 소비 페이지 변경 최소화
