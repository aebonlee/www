# DreamIT Biz - 2차 개발 작업내역 (2026-03-29)

**작업일**: 2026-03-29
**대상**: openclaw + 전체 프로젝트 공통 작업

---

## 1. OpenClaw 프로젝트 (8커밋)

### 1.1 모바일 접근성 개선

| 작업 | 커밋 | 상세 |
|------|------|------|
| .gitignore 표준화 | `684dc84` | .claude/, Windows 임시파일 제외 추가 |
| 모바일 반응형 그리드 | `74055ae` | Home 인라인 그리드 → CSS 클래스 추출, 다크모드 규칙 추가 |
| 모바일 2차/3차 메뉴 | `e71a83d` | 사이드바 토글, 아코디언 드롭다운, 344px 뷰포트 대응 |

**주요 변경**:
- OpenClawGuide 사이드바: 빈 `onClick={() => {}}` → `isSidebarOpen` state + 오버레이 백드롭
- Navbar: `openDropdown` state → 모바일 아코디언 드롭다운 구현
- NAV_ITEMS에 `children` 배열 추가 (OpenClaw 9개 섹션)
- URL 해시 기반 섹션 네비게이션 (`useLocation` + `useEffect`)
- 360px 미디어쿼리 추가, `body { min-width: 320px }`

### 1.2 다크모드 수정

| 작업 | 커밋 | 상세 |
|------|------|------|
| 하드코딩 컬러 40+개 제거 | `f963f69` | 인라인 hex → CSS 변수 전환 |

**수정 내용**:
- CSS 클래스 신규: `.oc-code-block`, `.oc-external-link`, `.oc-check-item`, `.oc-chip--blue/green/purple`, `.oc-badge--easy/medium/hard`
- content-pages.css: 라이트 모드 기본 스타일
- dark-mode.css: 다크 모드 오버라이드
- OpenClawGuide.jsx: 인라인 스타일 3개 객체 제거, 40+개 hex → CSS 변수
- 빌드 크기 감소: 55.22KB → 54.43KB (-780B)

### 1.3 미디어 쿼리 재구성

| 작업 | 커밋 | 상세 |
|------|------|------|
| 5단계 브레이크포인트 | `853b3f4` | 1100+1024 이중 → 1024 단일 통합 |

**최종 체계**:
```
Desktop     ≥1281px   전체 네비게이션
Compact     1024-1280  nav 축소 (font 14px, padding 8px 10px)
Tablet      768-1023   햄버거 메뉴 전환
Mobile      481-767    1열 레이아웃, 사이드바 슬라이드
Small       361-480    폰트/카드 축소
Mini        ≤360       344px 디바이스 커버
```

- `.nav-links`: `flex-wrap: nowrap; white-space: nowrap;` 추가

### 1.4 네비게이션 정리

| 작업 | 커밋 | 상세 |
|------|------|------|
| OpenClaw 드롭다운 제거 | `f88a378` | 데스크톱: 직접 링크, 모바일: 아코디언 유지 |
| 커뮤니티 드롭다운 제거 | `d9adf13` | 프롬프트 실습/갤러리 하위메뉴 삭제 |

### 1.5 인증 시스템 복구

| 작업 | 커밋 | 상세 |
|------|------|------|
| 프로필 자동 생성 | `22b57c9` | SQL 재설정 후 기존 유저 복구 |
| .env 파일 생성 | (미커밋) | Supabase URL/Key 환경변수 |

**AuthContext 수정**:
```javascript
// 프로필 없으면 자동 생성 (upsert)
if (!activeProfile || selectError) {
  const { data: { user: authUser } } = await supabase.auth.getUser();
  // auth 메타데이터로 프로필 자동 생성
}
```

**schema.sql 추가** (섹션 11):
```sql
-- 기존 유저 프로필 백필
INSERT INTO user_profiles (id, display_name, avatar_url, role)
SELECT au.id, COALESCE(...), COALESCE(...), 'user'
FROM auth.users au LEFT JOIN user_profiles up ON up.id = au.id
WHERE up.id IS NULL ON CONFLICT (id) DO NOTHING;
```

### 1.6 문서화

| 작업 | 커밋 | 상세 |
|------|------|------|
| 개발일지 + 평가보고서 | `cde3da4` | Day 2 작업 기록, 반응형 5단계 반영 |

---

## 2. 전체 프로젝트 공통 작업 (이전 세션)

### 2.1 장바구니/결제 버그 수정 (3개 프로젝트)

**대상**: edu-hub, papers, templete-ref

| 버그 | 수정 |
|------|------|
| `_memoryOrders` TDZ 위험 | 선언을 파일 상단으로 이동 |
| 장바구니 합계 NaN | `(Number(price) \|\| 0) * (Number(qty) \|\| 0)` 가드 |
| 수량 + 버튼 max 없음 | `disabled={item.quantity >= 99}` 추가 |

### 2.2 히어로 반응형 추가 (20개 프로젝트)

**대상**: koreatech, allthat, reserve, papers, self-branding, marketing, uxdesign, digitalbiz, db-study, edu-hub, data-structure, linux-study, templete-ref, coding, ai-prompt, ai-data, software, docs, teaching, python-study

**추가된 미디어쿼리**: hero.css에 1024px/768px/480px 반응형 블록 추가

### 2.3 digitalbiz 404 수정
- 404.html SPA 리다이렉트 스크립트 추가

---

## 3. 빌드 & 배포 현황

| 프로젝트 | 빌드 | 커밋 | 푸시 | 배포 |
|----------|------|------|------|------|
| openclaw | OK (4.31s) | cde3da4 | OK | gh-pages |
| edu-hub | OK | OK | OK | gh-pages |
| papers | OK | OK | OK | gh-pages |
| templete-ref | OK | OK | OK | gh-pages |
| + 20개 프로젝트 hero 수정 | OK | OK | OK | gh-pages |

---

*작성: Claude Opus 4.6*
*프로젝트 관리: Ph.D Aebon Lee, DreamIT Biz*
